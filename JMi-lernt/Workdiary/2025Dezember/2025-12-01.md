[[2025-11-28]] <->[[2025-12-02]]

| Time  | Termin      |
| ----- | ----------- |
| 7:45  | Kommen Home |
| 13:15 | Azubi Daily |
| 14:00 | Sync R&D    |
| 16:20 | Gehen       |
- [x] Datenschutz Erklärung
- [ ] Meine Timeline Prüfung [[Timeline Prüfung]]
	- [ ] Was brauche ich dafür
		- [ ] Kalender welches Plug in nutze ich
		- [ ] Luggage und JMi-lernt als getrennte Vaults
# Aufgaben
- [x] Hangman
	- [x] Spielablauf
	- [x] Zufallsworte
- [ ] PBI :Countdown
	- [x] Beschreibung
	- [x] Akzeptanzkriterien
	- [ ] Umsetzung
		- [ ] Pseudo Code
# Hangman
#funktion #algorithm #Schleife #while #workflow
### Beschreibung
Ein Galgenmännchenspiel programmieren.
1. Spiel ans Laufen bekommen
2. Liste mit Wörtern, aus denen zu fällig eins ausgelesen wird.
#### Spielablauf
Der Speile bekommt ein Reihe von Platzhaltern für jeden Buchstaben gezeigt.
Der Spieler wird aufgefordert einen Buchstaben einzugeben.
Der eingegebene Buchstabe wird vom Rechener mit dem Wort verglichen.
Wenn der Buchstabe vorkommt wird der neue Sting eingeblendet und eine neue Eingabe erfragt.
Wenn der Buchstabe nicht vorkommt wird von 7 runter gezählt, der String bleibt unverändert und es wird eine neue Eingabe erfragt.
Spiel Endet wenn der Spieler das Wort erraten hat oder der Counter bei Null angekommen ist.
#### Wie mache ich das:
#workflow 
*erst die eine Funktion/Abschnitt fertig machen. Hüpfen ist tödlich, der Ablauf ist an keiner Stelle gesichert. **Trau dich Schritte zu beenden***
*Von innen nach außen oder von außen nach innen arbeiten.
Nicht mischen.*

Es gibt einen AusgangsString mit dem Wort.
Es gibt einen AusgabeString:
Der besteht aus Platzhaltern `*` für die Buchstaben `len()`
Schleife
Der `input()` wird mit dem Ausgangsstring verglichen
der Ausgabestring wird neu aufgebaut
- Das vorhandene Zeichen wird beibehalten. Es kann Buchstabe aus der vorherigen Runde sein.
	- `*` wenn der Buchstabe nicht gefunden wurde
- der Buchstabe  wenn er gefunden wurde

der Zähler wird runtergezählt
Wenn die neue Ratereihe erstellt wurde wird sie mit dem Begriff verglichen. das ist eine ``while Schleife``
Sind sie gelich GEwonnen 
Sind sie nicht gleich -1
neuer Input 
zaehler = 0 verloren
Ausgabestring wird angezeigt und neue Eingabe erwartet.
Groß und Kleinschreibung spielt keine Rolle.
### Pseudocode
`Begriff = "zuratendes Wort"`
`Begriff.upper()`
`Begtiff.len()` -> brauche ich nicht, einfacher Austausch mit For
`RateReihe = Platzhalter länge Begriff`
`print(RateReihe)`
`Zähler = 7`
`Für Zähler != 0:`

`BuchstabeGeraten = input()`
`BuchstabeGeraten.upper()`
`Für jedes Zeichen in Begriff`
	`wenn BuchstabeGeraten in Begriff`
		 `+ BuchstabeGeraten` 
	`Sonst` 
		`+ Ratereihe Zeichen`
		`Zähler -1`
`RateReihe ausgeben`
`Neue eingab`

`Wenn Begriff = RateReihe` 
`print Gewonnen`
`Wenn Zähler == 0 und`
`RateReihe !=Begriff`
`print leider verloren! Noch mal?`

- [ ] Der Pseudocode ist unaufgeräumt. Rechtschreibung und Gliederung kontrollieren.
### Durchführung
#### Eingabe Aufforderung:
"`Lass und Hangman spielen.`
`Kannst du dieses Wort erraten:" + RateReihe + "Gib einen Buchstaben ein "`
`Buchstabe = input()`

#### Spielstart
`zaehler = 7`
`solange Begriff == RateReihe  = False
`Buchstabe = buchstabe.upper()`
`Für jedes Zeichen im Begriff`
	`wenn Buchstabe == Zeichen`
	`rateReihe = rateReihe + Buchstabe`
		`sonst rateREihe = rateReihe + ZeichenRatereihe`


`zaehler = zahler -1`

			
Fälle und ihre ERgebnisse:
b nicht in Begriff
	rateReihe bleibt gleich ![[AustauschUmlautenLösung.py]]
### SpielVerlauf
-  in der If-Abfrage `(buchstabeSpiel not in begriff):
	- wird mit `ergbnisReihe = rateReiheSpiel` das ERgebnis überschreiben
- die Else hängt nur einfach Buchstaben an.
	- das gibt kuddel-Muddel
	- ERst die Else lösen dann die if.

#### die Zeichne arten
Ich habe 3 Arten von Zeichen:
	- Den eingegebenen Buchstaben b
	- Den Buchstaben im rateBegriff r
	- Die Zeichen der rateReihe
		- Die unterscheiden sich in Platzhalter p
		- erratenen Buchstaben e
			- die e darf ich nicht vergessen, sie müssen immer erhalten bleiben.
Ich muss den Buchstaben b an der richtigen Stelle platzieren.
wie mache ich das ich gehe den Begriff durch und schaue, 
	ob b == r
		dann wird b zu e und wird in ergebnisReihe eingefügt
	wenn b !=r bleibt ander stelle das zeichen in der ergebnisreihe.
	Wo befinde ich mich im String? Beide Strings müssen paralellaufen.
		index zählen für beide
		Was und wie zähle ich?
		index variable als counter
		ich zähle die Zeichenstellen  in der RateReihe.
		für jede Stelle schreibe ich das Zeichen in den ERgebnisstring
		außer ich füge den erratenen Buchstaben ein.
		für jeden Buchstaben des Begriffs schreibe ein zeichen von der Rate reiche in die Ergebnissreihe.
		zähle den Index der rateReihe einen hoch
		`for r in begriff:
			`wenn r != b 
			
			`z = rateReihe[index]`
			`ergebnissReihe = ergebnissReihe + z
			index = index + 1
	
#### Was weiß ich an der Stelle?
ich weis das der buchstabeSpiel im begriff vorkommet.
ich weis nicht an welcher Stelle. 
Diese muss ich finden. 
ist der buchstabeBegriff an indexstelle  gleich dem buchstabenSpiel?
 ja
 dann schreibe den buchstabe BEgriff in die ERgebnisreihe
 index + 1
 nein 
 dann schreibe das zeichenRateReihe mit dem gelichen Index  in die ERgebnisreihe.
- [x] index wird mit gezählt
- [x] break für Gewonnen funktioniert.
- [ ] mehrfaches Vorkommen von Buchstaben wird nicht erkannt
- [x] wechsel von ein erster if in else funktioniert nicht.
- [ ] der Index funktioniert nicht, wenn der gerateneBuchstabe an nter stelle steht.
- ich muss bestimmen an welcher STelle der richtigGeratene Buchstabe vor kommt.
#### Vorschlag von GEorg
`wunderbar`

`_________`
##### Ansatz 1:  ergebnisReihe wird zusammen gestückelt.
- in einer Forschleife
	- wird nach geratenenBuchstaben gesucht
		- wie oft kommt er vor find()
		- wo steht er index()
- mit den Informationen wird ergebnisReihe gesliced
	- und der geratene Buchstabe eingefügt.
	- bei mehrfachem vorkommen
		- muss von der Einfügestelle aus noch mal der index ermittelt werden
		- um den Buchstaben noch mal ein zu fügen.
`=> r`

`für alle Buchstaben in Begriff:`  
  `wenn buchstabe r ist, dann:`  
    `Platzhalter an Stelle x ersetzen`

`platzhalter ausgeben`

`Begriff[x] + r + Begriff[::x]`

`__________`

`______r___`  
`______r__ + r +`   
`______r__r`


##### zweiter Ansatz: der ergbnisString wird immer neu aufgebaut.
- dazu werden gerateneBuchstaben in einer eigenen Stringvariablen gemerkt.
- dann werden alle Buchstaben des Begriffs mir dem geratenenString verglichen
	- wenn ja 
		- dann ergebnisString = ergbnisString + buchstabe
		- sonst ergebnisSring = ErgbnisString + Platzhalter
		- Die Doppelung von Buchstaben wird durch das duchgehen des Begriffs abgedeckt.
`Begriff = "wunderbar"`  
`geratenerString = ""`

`geraten = "raw"`

`für alle Buchstaben in Begriff:`  
  `wenn buchstabe in geraten vorhanden ist:`  
    `geratenerString = geratenerString + buchstabe`  
  `ansonsten:`  
    `geratenerString = geratenerString + "_"`  
  
`w____r_ar`
		
- [ ] meine Variablen wie verändern sie ich
- [ ] wie benenne ich sie
- [ ] wer enthält was.
`## Stringvariablen`
`begriff = "wunderbar"`
`begriff = begriff.upper()`
`rateReihe = "" # Der Sting mit den Platzhaltern den der Spieler angezeigt bekommt`
`ergebnisReihe = "" # Der Sting in den der geratene Buchstabe eingefügt ist`
`buchstabenEingegeben ="" # Der String in dem das Programm sich merkt welche Buchsaben eingegeben wurden. Diesen sieht der Spieler nicht`
`## Zeichenvariablen`
`buchstabe = "" # Zeichen das der Spieler eingibt`
`platzHalter = "*" # Zeichen in der rateReihe und der Ergebnisreihe für noch nicht erreatene Buchstaben`
`geraten = "" # Buchstaben in buchstabenEingegeben`

#### Teile des Spielverlaufs
Klammer 1
`while zaehler != 0`
	
Klammer 2 a
	`if: leider falsch
		`zaehler -1`
	``else:
Klammer 3
		`for r in begriff`
	Klammer 4
			`if r in buchstabenEingegebn
				`an ergebnis hängen`
			`sonst Platzhalter anhängen` 
Klammer 2 b	
	`if Gewonnen`
`else: Verloren`
## Ergebnis 
Ich habe mich sehr schwergetan. Ich habe den ÜBerblick über meine Variablen verloren. Das führt zum stolpern. Mehr mit trail und error gearbeitet als mit Lösungsstartegie. Grafik nicht genutzt. Keine Entscheidung getroffen sondern los gelegt. Konnte miene eignen Schritte nicht nach vollziehen
Hier ist ein guter Überblick für den #workflow 
[[2025-11-25#Lösungsansätze entwickeln]]
**Keep it simple**
## Zufällige Wortauswahl
### Beschreibung
Um das Spiel spannender zu machen. Wählt das Programm zufällig aus einer Liste von Worten aus.
### Was wird gebraucht
- Liste mit Worten
- zufällige Auswahl
	- das habe ich im PWgenerator gemacht
- funktion die die Auswahl trift
	- und diese als variabel begriff übergibt
### Wie will ich das machen
- Liste definieren.
- Funktion in eigener Datei entwickeln
	- Warum es ist nicht viel
- Ich die Spieldatei einfügen.
# PBI /Arbeitspakete
#pbi #workflow 
[[Arbeitspakete aka PBI]]
# Spiel: Wort aus zufälligen Buchstaben bilden
#pbi #workflow #algorithm 
[[2025-12-01#Spiel Wort aus zufälligen Buchstaben bilden]]
## Beschreibung
Es wird ein Spiel programmiert. Das Spiel ist ein Wortspiel, bei dem der Spieler aus einer Reihe von zufälligen Buchstaben ein möglichst langes, englisches Wort bildet.

[[2025-12-01#Beschreibung]]
## Akzeptanzkriterien
- Der String wird nach den Vorgaben erstellt und angezeigt.
- Die Prüfung des eingegeben Worts erfolgt nach den oben beschriebenen Regeln.
- Dem Benutzer wird kurz und verständlich erklärt, was seine Aufgabe ist.
- Die Punkte werden richtig berechnet und angezeigt.
- Eingabe von Zahlen oder Sonderzeichen wird nicht akzeptiert.
## Durchführung
### Ähnliche Aufgaben
[[2025-12-01#Ähnliche Aufgaben]]
#### Was brauche ich wo für
- range(9)
- len() - länge des String
- zaehler für Vokale und Konsonanten
- import random - randint() - zufällige Buchstaben
- Liste mit Vokalen - Liste mit Konsonanten
- for schleife für die Auswertung
- if else für die Auswertung

### Pseudocode
Programmteile:
- Stringerstellung
`ListeVokale`
`ListeKonsonanten`
`Buchstaben Auswahl Listen = [ListeVokale,ListeKonsonanten`
`Für Buchstabe in range(9)`
	`ListeVokal oder Listekonsonant`
		`einen Buchstaben auswählen
		`if vokal + 1
		`else Konsonant +1`
		*Für die richtige Aufteilung der Buchstaben, muss ich mir noch mehr Gedanken Machen*
`String = String + Buchstab`

- Aufgabestellung an den Benutzer
print(Spielregeln)
print(String)
input()

- Benutzereingabe
eingabe Wort
Wort.upper()

 - Auswertung
 len(Wort) - >9 Fehler
 für jeden Buchstaben in String
	 wenn buchstabe im Wort
	 *wie und was für möglichkeiten habe ich das zu prüfen.
	 jeder Buchstabe nur einmal verwenden. Aus dem string löschen? In einen Lösungsstring schreiben, VERschiedene Bauchstaben im String und im Wort zählen.*
	 wenn buchstabe nicht im Wort
		 Fehler
wenn das wort gültig ist
	len(Wort) = Punktzahl
	wenn länge(Wort) = 9 Punktzahle = 18