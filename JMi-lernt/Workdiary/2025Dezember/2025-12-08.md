[[2025-12-05]] <- 2025-12-08 - > [[2025-12-09]]
# Zeiten
- 07:45 Kommen home
- 13:00 - 14:30 H1'26 Kick Off
- 13:15 Azubi Daily
- 14:00 R&D Sync
- geplant 16:30 gehen
# Backlog
- [ ] Planung bis Weihnachten
	- [ ] Urlaubstage verbrauchen?
	- [x] Java lernen
- [ ] Berichtsheft
	 - [ ] Tamplate das für mich funktioniert
		 - [ ] Von Aktuell nach alt
		 - [ ] Wenn etwas Zeit duchgehen
		 - [ ] 
	 - Wie sieht das aus
- [ ]  [[Timeline Prüfung]]
	- [ ] Ansprechpartner
	- [ ] Anforderungen
	- [x] Termine
	- [ ] Informationsordner
		- [ ] Was habe ich wo
		- [ ] wo soll es hin
- [x]  call AA wegen Weiterbildungsprämie
	- [ ] RR erwartet bis Di. - Anonym
# Aufgaben
- [x] Verbesserung des Wortspiel-Codes
- [x] Java
	- [x] installation des JDK -Java Divelopment Kid
	- [x] Grundlagen
		- [x] Progamm Blöcke
		- [ ] scanner und System.out.print()

# Code verbessern
#workflow
- Funktionen nutzen
	- Vermeiden den gleichen Code an mehreren Stellen zu nutzen
	- Änderungen nur an einer Stelle machen
	- Übersichtlicher
- Dadurch ist auch möglich Vererbung zu nutzen
-  Code so gestallten, dass Funktionen oder Codeblöcke einzeln auf ihre Funktion getestet werden können.
# Java Grundlagen
#JAVA #package #classes 
## Syntax
- Java ist ein Compilersprache.
- Klassen und Dateinamen sind immer gleich.
- eine Klasse beginnt mit `public class NAME`
- Jede Klasse beginnt mit `public static void main()`
- Variablen müssen einen Typ haben.
- Was aufgemacht wird muss auch wieder geschlossen werden. `{}`
- Am Ende eines Befehls steht ein `;`
- - Java ist cassesensetive
## Library Package Class
#attribute #package 
- Mehrere zusammengehörige `classen` ergeben ein `package` ähnlich einem Ordner
	- Es gibt viele packages für java. 
	- man kann sie selbst erstellen
	- sie werden mit `importe package.name.Class;` oder` import package.name.*; ` importiert
	- built-in packages kommen von der Java APi
		- die ist eine libery die in mehrere packages aufgeteilt ist
- `java.util.*` ist eins der am häufigsten verwendeten packages 
-  Klassennamen beginnen mit Großbuchstaben
### Attribute
- Klassen Attribute sind Variablen innerhalb einer Klasse
	- Sie werden auch `field` genannt
	`public class Main` 
	`{`
		`int x = 5;`
	  `int y = 3;`
	`}`
- Aufgerufen werden Attribute in dem ein Objekt erstellt wird und mit `.` ausgegeben wird.
```java
	Main myObj = new Main();
    System.out.println(myObj.x);
```
- Da Attribute Variablen sind können sie in Objekten überschreiben werden.
	- Überschreiben wird mit dem Keyword `final` verhinder
## Schleifen
#Schleife #for
### for
- begrenzte Anzahl an Durchläufen
- syntax `for (statment 1; statment 2; statment3)
	- statment1 - Variabeln definition -Startbedingung wird einmal zu Beginn ausgeführt
	- statment2 -Abbruchbedingung Kondition für die Durchführung 
	- statment3 -wird am ende jeder Schleife ausgeführt.
		- lauf Variable
### for-each
#for-each
- wird für die Elemente eines `array` genutzt
- das Array muss vordem Beginn der Schleife definiert sein
- es werden zwei Variablen benutzt (TeildesArray : Array)
### while
#while 
- while Schleifen werden ausgeführt so lange eine Bedinung `true` ist
- `while (condition) {code}`
	- der Counter steht am Ende des Codes
	- Variablen vorher definieren
### do while
#fussgesteuerteSchleife
- Diese Schleife wird immer einmal ausgeführt. Die Bedingung wird am Ende des Codeblocks geprüft.
- `do { codeblock } while (contition) ;`
- Der Counter steht wieder mit im Codeblock
- Auch wen die Bedindung `false` ist wird der Code einmal ausgeführt.
## Abfragen
### if else
#ifelse
- condition muss `true` oder `false` sein
	- oft mit Vergeleichsoperatoren
	- if Block: wenn eine Bedingung true ist
		- `if (condition){codeblock}`
		- `boolean` Variablen können ohne Operator in die `condition` geschrieben weren
			- `if(isLightOn == true)`ist das gleiche wie `if(isLighton)`
	- else Block: wenn die gliche Bedingung false ist
		- `if(condition)`
			- `{codeblock}`
		- `else` 
			- `{anderer CodeBlock}`
	- else if Block: eine neue Bedingung wenn die erste Bedingung falsch ist.
	`if (Condition)`
		`{codeblock}`
	`else if `
		`{ codeblock}`
	`else`
		`{codeblock}`
	- Es gibt auch eine verkürzte Schreibweise.
	- `variable = (condition)? expressionTrue : expressionFalse;`
	- Für kurze Auswahlen z.B auch in `System.out.println()`
### switch
#switchcase #matchcase
- Wie bei Match case in Python gibt es in Java `switch case` um einzelne Ergebnisse Fälle zu erreichen
- Nach der Code für einen bestimmten Fall ausgeführt wurde wird das Statment abgebrochen
- der Ausdruck wird nur einmal ermittelt und mit den WErten `case values ` verglichen
	`switch (expression){`
		`case x:`
			`codeblock`
			`break;`
		`case y:`
			`codeblock`
			`break;`
		`default:`
		`}`
## Durchführung
Eigener Ordener 
ergibt ein eigenes `package`
