[[2025-11-27]] <-> 

| Zeit  | Termin           |
| ----- | ---------------- |
| 7:30  | Kommen           |
| 13:00 | Joint            |
| 13.15 | Azubi            |
| 16:00 | Telekom (privat) |
- [ ] Datenschutz Erklärung
- [ ] Meine Timeline Prüfung [[Timeline Prüfung]]
# Aufgaben
- [ ] Hangman
	- [ ] Spielablauf
	- [ ] Zufallsworte
# Hangman
#funktion #algorithm #Schleife #while #workflow
### Beschreibung
Ein Galgenmännchenspiel programmieren.
1. Spiel ans Laufen bekommen
2. Liste mit Wörtern, aus denen zu fällig eins ausgelesen wird.
#### Spielablauf
Der Speile bekommt ein Reihe von Platzhaltern für jeden Buchstaben gezeigt.
Der Spieler wird aufgefordert einen Buchstaben einzugeben.
Der eingegebene Buchstabe wird vom Rechener mit dem Wort verglichen.
Wenn der Buchstabe vorkommt wird der neue Sting eingeblendet und eine neue Eingabe erfragt.
Wenn der Buchstabe nicht vorkommt wird von 7 runter gezählt, der String bleibt unverändert und es wird eine neue Eingabe erfragt.
Spiel Endet wenn der Spieler das Wort erraten hat oder der Counter bei Null angekommen ist.
#### Wie mache ich das:
#workflow 
*erst die eine Funktion/Abschnitt fertig machen. Hüpfen ist tödlich, der Ablauf ist an keiner Stelle gesichert. **Trau dich Schritte zu beenden***
*Von innen nach außen oder von außen nach innen arbeiten.
Nicht mischen.*

Es gibt einen AusgangsString mit dem Wort.
Es gibt einen AusgabeString:
Der besteht aus Platzhaltern `*` für die Buchstaben `len()`
Schleife
Der `input()` wird mit dem Ausgangsstring verglichen
der Ausgabestring wird neu aufgebaut
- Das vorhandene Zeichen wird beibehalten. Es kann Buchstabe aus der vorherigen Runde sein.
	- `*` wenn der Buchstabe nicht gefunden wurde
- der Buchstabe  wenn er gefunden wurde

der Zähler wird runtergezählt
Wenn die neue Ratereihe erstellt wurde wird sie mit dem Begriff verglichen. das ist eine ``while Schleife``
Sind sie gelich GEwonnen 
Sind sie nicht gleich -1
neuer Input 
zaehler = 0 verloren
Ausgabestring wird angezeigt und neue Eingabe erwartet.
Groß und Kleinschreibung spielt keine Rolle.
### Pseudocode
`Begriff = "zuratendes Wort"`
`Begriff.upper()`
`Begtiff.len()` -> brauche ich nicht, einfacher Austausch mit For
`RateReihe = Platzhalter länge Begriff`
`print(RateReihe)`
`Zähler = 7`
`Für Zähler != 0:`

`BuchstabeGeraten = input()`
`BuchstabeGeraten.upper()`
`Für jedes Zeichen in Begriff`
	`wenn BuchstabeGeraten in Begriff`
		 `+ BuchstabeGeraten` 
	`Sonst` 
		`+ Ratereihe Zeichen`
		`Zähler -1`
`RateReihe ausgeben`
`Neue eingab`

`Wenn Begriff = RateReihe` 
`print Gewonnen`
`Wenn Zähler == 0 und`
`RateReihe !=Begriff`
`print leider verloren! Noch mal?`

- [ ] Der Pseudocode ist unaufgeräumt. Rechtschreibung und Gliederung kontrollieren.
### Durchführung
#### Eingabe Aufforderung:
"`Lass und Hangman spielen.`
`Kannst du dieses Wort erraten:" + RateReihe + "Gib einen Buchstaben ein "`
`Buchstabe = input()`

#### Spielstart
`zaehler = 7`
`solange Begriff == RateReihe  = False
`Buchstabe = buchstabe.upper()`
`Für jedes Zeichen im Begriff`
	`wenn Buchstabe == Zeichen`
	`rateReihe = rateReihe + Buchstabe`
		`sonst rateREihe = rateReihe + ZeichenRatereihe`


`zaehler = zahler -1`

			
Fälle und ihre ERgebnisse:
b nicht in Begriff
	rateReihe bleibt gleich ![[AustauschUmlautenLösung.py]]
### SpielVerlauf
-  in der If-Abfrage `(buchstabeSpiel not in begriff):
	- wird mit `ergbnisReihe = rateReiheSpiel` das ERgebnis überschreiben
- die Else hängt nur einfach Buchstaben an.
	- das gibt kuddel-Muddel
	- ERst die Else lösen dann die if.

#### die Zeichne arten
Ich habe 3 Arten von Zeichen:
	- Den eingegebenen Buchstaben b
	- Den Buchstaben im rateBegriff r
	- Die Zeichen der rateReihe
		- Die unterscheiden sich in Platzhalter p
		- erratenen Buchstaben e
			- die e darf ich nicht vergessen, sie müssen immer erhalten bleiben.
Ich muss den Buchstaben b an der richtigen Stelle platzieren.
wie mache ich das ich gehe den Begriff durch und schaue, 
	ob b == r
		dann wird b zu e und wird in ergebnisReihe eingefügt
	wenn b !=r bleibt ander stelle das zeichen in der ergebnisreihe.
	Wo befinde ich mich im String? Beide Strings müssen paralellaufen.
		index zählen für beide
		Was und wie zähle ich?
		index variable als counter
		ich zähle die Zeichenstellen  in der RateReihe.
		für jede Stelle schreibe ich das Zeichen in den ERgebnisstring
		außer ich füge den erratenen Buchstaben ein.
		für jeden Buchstaben des Begriffs schreibe ein zeichen von der Rate reiche in die Ergebnissreihe.
		zähle den Index der rateReihe einen hoch
		`for r in begriff:
			`wenn r != b 
			
			`z = rateReihe[index]`
			`ergebnissReihe = ergebnissReihe + z
			index = index + 1
	
#### Was weiß ich an der Stelle?
ich weis das der buchstabeSpiel im begriff vorkommet.
ich weis nicht an welcher Stelle. 
Diese muss ich finden. 
ist der buchstabeBegriff an indexstelle  gleich dem buchstabenSpiel?
 ja
 dann schreibe den buchstabe BEgriff in die ERgebnisreihe
 index + 1
 nein 
 dann schreibe das zeichenRateReihe mit dem gelichen Index  in die ERgebnisreihe.
- [x] index wird mit gezählt
- [x] break für Gewonnen funktioniert.
- [ ] mehrfaches Vorkommen von Buchstaben wird nicht erkannt
- [x] wechsel von ein erster if in else funktioniert nicht.
- [ ] der Index funktioniert nicht, wenn der gerateneBuchstabe an nter stelle steht.
- ich muss bestimmen an welcher STelle der richtigGeratene Buchstabe vor kommt.
#### Vorschlag von GEorg
`wunderbar`

`_________`
Ansatz 1
die ergebnisReihe wird zusammen gestückelt.
in einer Forschleife
wird nach geratenenBuchstaben gesucht
wie oft kommt er vor find()
wo steht er index()
mit den Informationen wird ergebnisReihe gesliced und der geratene Buchstabe eingefügt.
bei mehrfachem vorkommen, muss von der einfügestelle aus noch mal der index ermittelt werden um den Buchstaben noch mal ein zu fügen.
`=> r`

`für alle Buchstaben in Begriff:`  
  `wenn buchstabe r ist, dann:`  
    `Platzhalter an Stelle x ersetzen`

`platzhalter ausgeben`

`Begriff[x] + r + Begriff[::x]`

`__________`

`______r___`  
`______r__ + r +`   
`______r__r`

`=========================================`
zweiter Ansatz
der ergbnisString wird immer neu aufgebaut.
dazu werden gerateneBuchstaben in einer eigenen Stringvariablen gemerkt.
dann werden alle Buchstaben des Begriffs mir demgeratenenString verglichen
wenn ja dann ergebnisString = ergbnisString + buchstabe
sonst ergebnisSring = ErgbnisString + Platzhalter
Die Doppelung von Buchstaben wird durch das duchgehen des Begriffs abgedeckt.
`Begriff = "wunderbar"`  
`geratenerString = ""`

`geraten = "raw"`

`für alle Buchstaben in Begriff:`  
  `wenn buchstabe in geraten vorhanden ist:`  
    `geratenerString = geratenerString + buchstabe`  
  `ansonsten:`  
    `geratenerString = geratenerString + "_"`  
  
`w____r_ar`
		
- [ ] meine Variablen wie verändern sie ich
- [ ] wie benenne ich sie
- [ ] wer enthält was.
strings:
Begriff
rateReihe
ergebnisReihe
?geratenenBuchstaben?
Zeichen:


